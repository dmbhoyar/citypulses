<h2>Services</h2>
<%= link_to 'New Service Record', new_service_path, class: 'button' %>
<ul>
  <% @services.each do |service| %>
    <li>
      <%= link_to service.title, service_path(service) %>
      <span>City: <%= service.city.name if service.city %></span>
    </li>
  <% end %>
</ul>
